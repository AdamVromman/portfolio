---
import "../CSS/background.css";
---

<div id="background">
  <svg id="background_svg">
    <g id="starting-animation-dots"></g>
    <g id="background_home_lines-group"></g>
  </svg>
</div>
<script>
  import gsap from "gsap";
  import {
    calculateNrOfRows,
    calculateTileWidth,
    getNrOfColumns,
  } from "../assets/Functions";
  import { selectedProject } from "../assets/Functions";

  const background = document.getElementById("background");
  const backgroundSVG = document.getElementById("background_svg");
  const timeline = gsap.timeline({
    delay: 1.5,
  });
  const linesGroup = document.getElementById("background_home_lines-group");

  // const drawDots = () => {
  //   const svgNS = "http://www.w3.org/2000/svg";
  //   const localTileWidth = calculateTileWidth();
  //   const nrOfRows = calculateNrOfRows(localTileWidth);
  //   const nrOfColumns = getNrOfColumns();
  //   const dotsGroup = document.getElementById("starting-animation-dots");
  //   const dotsAmount = getNrOfColumns() * nrOfRows;

  //   let counterRows = 0;
  //   let counterColumns = 0;

  //   if (dotsGroup) {
  //     for (let i = 0; i < nrOfColumns + nrOfRows - 2; i++) {
  //       const dot = document.createElementNS(svgNS, "circle");
  //       dot.classList.add("background_svg_dot");
  //       dot.setAttribute("cx", `${localTileWidth * i}px`);
  //       dot.setAttribute("cy", `${localTileWidth * i}px`);
  //       dot.setAttribute("r", "4px");
  //       dotsGroup.appendChild(dot);
  //     }
  //     //   while (counterRows < nrOfRows || counterColumns < nrOfColumns) {
  //     //     console.log("counterRows", counterRows);
  //     //     console.log("counterColumns", counterColumns);

  //     //     for (let j = 0; j < Math.max(counterRows, counterColumns) + 1; j++) {
  //     //       const dot = document.createElementNS(svgNS, "circle");
  //     //       dot.classList.add("background_svg_dot");
  //     //       dot.setAttribute("cx", `${localTileWidth * j}px`);
  //     //       dot.setAttribute(
  //     //         "cy",
  //     //         `${localTileWidth * (Math.max(counterRows, counterColumns) - j)}px`
  //     //       );
  //     //       dot.setAttribute("r", "4px");
  //     //       dotsGroup.appendChild(dot);
  //     //     }
  //     //     if (counterRows < nrOfRows) counterRows++;
  //     //     if (counterColumns < nrOfColumns) counterColumns++;
  //     //   }

  //     //   while (counterRows > 0 || counterColumns > 0) {
  //     //     console.log("counterRows", counterRows);
  //     //     console.log("counterColumns", counterColumns);

  //     //     for (let j = 0; j < Math.min(counterRows, counterColumns) + 1; j++) {
  //     //       const dot = document.createElementNS(svgNS, "circle");
  //     //       dot.classList.add("background_svg_dot");
  //     //       dot.setAttribute(
  //     //         "cx",
  //     //         `${localTileWidth * (nrOfRows - counterRows + j)}px`
  //     //       );
  //     //       dot.setAttribute(
  //     //         "cy",
  //     //         `${localTileWidth * (Math.min(counterRows, counterColumns) - j)}px`
  //     //       );
  //     //       dot.setAttribute("r", "4px");
  //     //       dotsGroup.appendChild(dot);
  //     //     }
  //     //     if (counterRows > 0) counterRows--;
  //     //     if (counterColumns > 0) counterColumns--;
  //     //   }
  //   }
  // };

  const drawLines = (animated: boolean = false) => {
    if (background && backgroundSVG && linesGroup) {
      const localTileWidth = calculateTileWidth();
      const nrOfRows = calculateNrOfRows(localTileWidth);
      background.style.width = `${getNrOfColumns() * localTileWidth}px`;
      background.style.height = `${nrOfRows * localTileWidth}px`;
      linesGroup.textContent = "";
      const svgNS = "http://www.w3.org/2000/svg";

      for (let i = 0; i < getNrOfColumns() / 2 - 1; i++) {
        const line = document.createElementNS(svgNS, "line");
        const x = `${localTileWidth * 2 + localTileWidth * 2 * i}px`;
        line.classList.add("background_svg_line");
        line.classList.add("line_vertical");
        line.classList.add("line_main");
        line.setAttribute("height", "0");
        line.setAttribute("x1", x);
        line.setAttribute("x2", x);
        line.setAttribute("y1", "0%");
        linesGroup.appendChild(line);
        if (animated) {
          timeline.to(
            line,
            {
              duration: 1,
              attr: { y2: "100%" },
              ease: "power4.out",
            },
            "<0.01"
          );
        } else {
          line.setAttribute("y2", "100%");
        }
      }

      for (let i = 0; i < getNrOfColumns() / 2; i++) {
        const line = document.createElementNS(svgNS, "line");
        const x = `${localTileWidth + localTileWidth * 2 * i}px`;
        line.classList.add("background_svg_line");
        line.classList.add("line_vertical");
        line.classList.add("line_sub");
        line.setAttribute("x1", x);
        line.setAttribute("x2", x);
        line.setAttribute("y1", "0");
        line.setAttribute("stroke-dasharray", "4");
        linesGroup.appendChild(line);
        if (animated) {
          timeline.to(
            line,
            {
              duration: 1,
              attr: { y2: "100%" },
              ease: "power4.out",
            },
            "<0.01"
          );
        } else line.setAttribute("y2", "100%");
      }

      for (let i = 0; i < nrOfRows / 2 - 1; i++) {
        const line = document.createElementNS(svgNS, "line");
        const y = `${localTileWidth * 2 + localTileWidth * 2 * i}px`;
        line.classList.add("background_svg_line");
        line.classList.add("line_horizontal");
        line.classList.add("line_main");
        line.setAttribute("y1", y);
        line.setAttribute("y2", y);
        line.setAttribute("x1", "0");
        linesGroup.appendChild(line);
        if (animated) {
          timeline.to(
            line,
            {
              duration: 1,
              attr: { x2: "100%" },
              ease: "power4.out",
            },
            "<0.01"
          );
        } else line.setAttribute("x2", "100%");
      }

      for (let i = 0; i < nrOfRows / 2; i++) {
        const line = document.createElementNS(svgNS, "line");
        const y = `${localTileWidth + localTileWidth * 2 * i}px`;
        line.classList.add("background_svg_line");
        line.classList.add("line_horizontal");
        line.classList.add("line_sub");
        line.setAttribute("stroke-dasharray", "4");
        line.setAttribute("y1", y);
        line.setAttribute("y2", y);
        line.setAttribute("x1", "0");
        linesGroup.appendChild(line);
        if (animated) {
          timeline.to(
            line,
            {
              duration: 1,
              attr: { x2: "100%" },
              ease: "power4.out",
            },
            "<0.01"
          );
        } else line.setAttribute("x2", "100%");
      }
    }
  };

  document.addEventListener("assetsloaded", () => {
    if (!selectedProject) drawLines(true);
  });

  const onResize = () => {
    if (!selectedProject) drawLines();
  };

  //drawDots();

  window.addEventListener("resize", onResize);
</script>
