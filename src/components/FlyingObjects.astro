---

---

<div id="flying-objects" class="absolute w-full h-full top-0 left-0 p-15 z-10">
  <div
    id="test"
    class="absolute z-0 top-0 left-0 bg-white border-2 border-project-trainworld"
  >
  </div>
  <div class="relative z-10 w-full h-full" id="canvas-container"></div>

  <script>
    import * as THREE from "three";
    import { DRACOLoader } from "three/addons/loaders/DRACOLoader.js";
    import { calculateNrOfRows, calculateTileWidth } from "../assets/Constants";
    import RAPIER from "@dimforge/rapier3d-compat";
    import Stats from "three/addons/libs/stats.module.js";

    let stats: Stats;

    await RAPIER.init();
    const gravity = new RAPIER.Vector3(0.0, 0.0, 0.0);
    const world = new RAPIER.World(gravity);
    const dynamicBodies: [THREE.Object3D, RAPIER.RigidBody][] = [];
    const frustumSize = 50;

    let renderer: THREE.WebGLRenderer,
      scene: THREE.Scene,
      camera: THREE.OrthographicCamera;

    const canvasContainer = document.getElementById("canvas-container");
    const test = document.getElementById("test");

    const projects = ["qausal"];

    const clock = new THREE.Clock();
    let delta;

    const createBox = () => {
      if (scene) {
        const restitution = 1;
        const translation = frustumSize / 2;
        const zTranslation = 2;
        const zRestitution = 0.1;
        const panelWidth = 50;
        const panelHeight = 0.5;

        const floorBody = world.createRigidBody(
          RAPIER.RigidBodyDesc.fixed()
            .setTranslation(0, -translation, 0)
            .setRotation({ w: 0, x: 0, y: 1, z: 0 })
        );
        const floorShape = RAPIER.ColliderDesc.cuboid(
          panelWidth,
          panelHeight,
          panelWidth
        ).setRestitution(restitution);
        world.createCollider(floorShape, floorBody);

        const floorBody2 = world.createRigidBody(
          RAPIER.RigidBodyDesc.fixed().setTranslation(0, translation, 0)
        );
        const floorShape2 = RAPIER.ColliderDesc.cuboid(
          panelWidth,
          panelHeight,
          panelWidth
        ).setRestitution(restitution);
        world.createCollider(floorShape2, floorBody2);

        const floorBody3 = world.createRigidBody(
          RAPIER.RigidBodyDesc.fixed().setTranslation(-translation, 0, 0)
        );
        const floorShape3 = RAPIER.ColliderDesc.cuboid(
          panelHeight,
          panelWidth,
          panelWidth
        ).setRestitution(restitution);
        world.createCollider(floorShape3, floorBody3);

        const floorBody4 = world.createRigidBody(
          RAPIER.RigidBodyDesc.fixed().setTranslation(translation, 0, 0)
        );
        const floorShape4 = RAPIER.ColliderDesc.cuboid(
          panelHeight,
          panelWidth,
          panelWidth
        ).setRestitution(restitution);
        world.createCollider(floorShape4, floorBody4);

        const floorBody5 = world.createRigidBody(
          RAPIER.RigidBodyDesc.fixed().setTranslation(0, 0, -zTranslation)
        );
        const floorShape5 = RAPIER.ColliderDesc.cuboid(
          panelWidth,
          panelWidth,
          panelHeight
        ).setRestitution(zRestitution);
        world.createCollider(floorShape5, floorBody5);

        const floorBody6 = world.createRigidBody(
          RAPIER.RigidBodyDesc.fixed().setTranslation(0, 0, zTranslation)
        );
        const floorShape6 = RAPIER.ColliderDesc.cuboid(
          panelWidth,
          panelWidth,
          panelHeight
        ).setRestitution(zRestitution);
        world.createCollider(floorShape6, floorBody6);
      }
    };

    const createFlyingObject = () => {
      const loader = new DRACOLoader();
      loader.setDecoderPath("/draco/");
      loader.preload();

      if (scene) {
        projects.forEach((project) => {
          loader.load(
            `/${project}.drc`,
            (geometry) => {
              geometry.center();
              geometry.scale(50, 50, 50);
              const material = new THREE.MeshStandardMaterial({
                color: 0x2a35c9,
                roughness: 0.1,
                metalness: 0.7,
              });
              const mesh = new THREE.Mesh(geometry, material);
              mesh.position.set(0, 0, 0);
              mesh.rotation.set(Math.random(), Math.random(), Math.random());
              scene.add(mesh);
              const projectBody = world.createRigidBody(
                RAPIER.RigidBodyDesc.dynamic()
                  .setTranslation(0, 0, 0)
                  .setCanSleep(false)
              );

              projectBody.setEnabledTranslations(true, true, false, false);
              const projectShape = RAPIER.ColliderDesc.cuboid(0.5, 0.5, 0.5)
                .setMass(100)
                .setRestitution(0);
              world.createCollider(projectShape, projectBody);

              projectBody.applyImpulse(
                new RAPIER.Vector3(
                  Math.random() * 1000 - 500,
                  Math.random() * 1000 - 500,
                  0
                ),
                true
              );

              dynamicBodies.push([mesh, projectBody]);
            },
            (xhr) => {
              console.log((xhr.loaded / xhr.total) * 100 + "% loaded");
            },
            (error) => {
              console.log(error);
            }
          );
        });
      }
    };

    const render = () => {
      if (renderer && scene && camera) renderer.render(scene, camera);
    };

    const animate = () => {
      requestAnimationFrame(animate);
      stats.update();

      delta = clock.getDelta();
      world.timestep = Math.min(delta, 0.1);
      world.step();

      for (let i = 0, n = dynamicBodies.length; i < n; i++) {
        const linVel = dynamicBodies[i][1].linvel();
        console.log(Math.abs(linVel.x) + Math.abs(linVel.y));
        if (Math.abs(linVel.x) + Math.abs(linVel.y) < 4) {
          dynamicBodies[i][1].applyImpulse(
            new RAPIER.Vector3(
              Math.random() * 1000 - 500,
              Math.random() * 1000 - 500,
              0
            ),
            true
          );
        }
        dynamicBodies[i][0].position.copy(dynamicBodies[i][1].translation());
        dynamicBodies[i][0].quaternion.copy(dynamicBodies[i][1].rotation());
      }

      render();
    };

    const init = () => {
      if (canvasContainer) {
        const localTileWidth = calculateTileWidth(canvasContainer);
        const NR_OF_ROWS = calculateNrOfRows(localTileWidth);
        const height = NR_OF_ROWS * localTileWidth;
        const aspect = canvasContainer.clientWidth / height;

        console.log(
          `localTileWidth: ${localTileWidth}, NR_OF_ROWS: ${NR_OF_ROWS}, height: ${height}, aspect: ${aspect}`
        );

        scene = new THREE.Scene();
        camera = new THREE.OrthographicCamera(
          (frustumSize * aspect) / -2,
          (frustumSize * aspect) / 2,
          frustumSize / 2,
          frustumSize / -2,
          0.1,
          100
        );

        camera.position.set(0, 0, 10);
        camera.lookAt(0, 0, 0);
        camera.updateProjectionMatrix();

        const light = new THREE.DirectionalLight(0xffffff, 3);
        light.position.set(1, 1, 1).normalize();
        scene.add(light);

        createFlyingObject();
        createBox();

        renderer = new THREE.WebGLRenderer({
          antialias: true,
          alpha: true,
        });

        renderer.setSize(canvasContainer.clientWidth, height);
        canvasContainer.insertAdjacentElement(
          "afterbegin",
          renderer.domElement
        );

        stats = new Stats();
        document.body.appendChild(stats.dom);
      }
    };

    const onWindowResize = () => {
      if (renderer && camera && canvasContainer) {
        const localTileWidth = calculateTileWidth(canvasContainer);
        const NR_OF_ROWS = calculateNrOfRows(localTileWidth);
        const height = NR_OF_ROWS * localTileWidth;
        if (test) {
          test.style.width = `${localTileWidth}px`;
          test.style.height = `${localTileWidth}px`;
        }

        camera.updateProjectionMatrix();

        renderer.setSize(canvasContainer.clientWidth, height);
      }
    };

    // if (test && canvasContainer) {
    //   test.style.width = `${calculateTileWidth(canvasContainer)}px`;
    //   test.style.height = `${calculateTileWidth(canvasContainer)}px`;
    // }

    init();
    requestAnimationFrame(animate);

    window.addEventListener("resize", onWindowResize);
  </script>
</div>
