import type { Shape } from "./Configurator";

interface TableLegsProps {
    selectedShape: Shape;
    selectedMaterial: string | null;
}


const TableLegs = ({ selectedShape, selectedMaterial }: TableLegsProps) => {
    return <mesh></mesh>
}


export default TableLegs